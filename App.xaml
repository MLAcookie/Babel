<Application x:Class="Babel.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:converters="clr-namespace:Babel.Converters"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Controls/ControlsDictionary.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Controls/StylesDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <converters:CountToVisibilityConverter x:Key="CountToVis" />
            <converters:NullToVisibilityConverter x:Key="NullToVis" />
            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="pack://application:,,,/app.ico"
                            ToolTipText="Babel Translator">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Settings" Click="TraySettings_Click" />
                        <Separator />
                        <MenuItem Header="Exit" Click="TrayExit_Click" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
